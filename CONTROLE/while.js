function getInteiroAleatorioEntre (min, mas) {
    cont valor - Math.random() * (max - min) + min
    return Math.floor (valor)
    }
    
    let opcao = 0
    
    while(opcao != -1) {
    opcao = getInteiroAleatorioEntre (-1, 10)
    console.log('Opção escolhida foi $(opcao).')
    }
    
    console.log('Até a próxima')
    
    // função while é própria para executar uma quantidade indeterminada de execuções.